description: |
  Startup LocalStack instance.

  This job installs LocalStack and starts it up in a local Docker container in the background.

executor: default

parameters:
  image-tag:
    type: string
    default: latest
    description: Tag of the localstack/localstack Docker image to use

steps:
  - startup
